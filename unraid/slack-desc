# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct. It's also
# customary to leave one blank line after the last line of description.

           |-----handy-ruler------------------------------------------------------|
pbc-wrapper: pbc-wrapper (proxmox-backup-client wrapper and static binaries)
pbc-wrapper:
pbc-wrapper: Provides statically-linked proxmox-backup-client and pxar binaries
pbc-wrapper: from the Proxmox project, together with pbc-wrapper: a Python
pbc-wrapper: script that loads PBS repository credentials from named .env
pbc-wrapper: profile files, keeping secrets out of shell history and scripts.
pbc-wrapper:
pbc-wrapper: Default profile directory: /etc/pbs-client/repos.d/
pbc-wrapper: Unraid users may also use /mnt/user/appdata/pbs/repos.d/
pbc-wrapper:
pbc-wrapper: https://github.com/jrarseneau/pbc-wrapper
